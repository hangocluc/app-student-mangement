import 'package:flutter/material.dart';

class AppRadius {
  static const Radius radius1 = Radius.circular(4);
  static const Radius radius2 = Radius.circular(8);
  static const Radius radius3 = Radius.circular(12);
  static const Radius radius4 = Radius.circular(16);
  static const Radius radius6 = Radius.circular(24);
  static const Radius radiusMax = Radius.circular(9999);
}
